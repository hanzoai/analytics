name: Create docker images (cloud)
# Neutralized — the native deploy pipeline is .hanzo/workflows/deploy.yml.
# GitHub is a mirror; the app image is built + pushed + deployed in-cluster
# by act_runner (BuildKit → ghcr.io/hanzoai/analytics → operator reconcile).
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
